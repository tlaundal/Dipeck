ext.sources = "src"
ext.tests = "test"

ext.nodejs = true

ext.compose = "notification"
afterEvaluate {
  composeBuild {
    inputs.file "healthcheck.js"
    inputs.file "package.json"
    inputs.dir "src/"
  }
}
